QUESTION :

The program must accept an integer N as the input. The program must print yes if N is a single digit integer, that is between 0 and 9 (inclusive). Otherwise, the program must print no as the output using a switch statement.

Example Input/Output 1:
Input:
8

Output:
yes

Example Input/Output 2:
Input:
735

Output:
no

ANSWER :

#include<stdio.h>
#include<stdlib.h>

void main()
{
int N;
scanf("%d",&N);
switch(N){
    case 0 ... 9:
    printf("yes");
    break;
    default:
    printf("no");
}
}
